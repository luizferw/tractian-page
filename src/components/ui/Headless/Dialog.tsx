"use client";

import {
  Dialog as HeadlessDialog,
  DialogBackdrop as HeadlessDialogBackdrop,
  DialogPanel as HeadlessDialogPanel,
  DialogTitle as HeadlessDialogTitle,
  TransitionChild as HeadlessTransitionChild,
} from "@headlessui/react";

export const Dialog = HeadlessDialog;
export const DialogBackdrop = HeadlessDialogBackdrop;
export const DialogPanel = HeadlessDialogPanel;
export const DialogTitle = HeadlessDialogTitle;
export const TransitionChild = HeadlessTransitionChild;
